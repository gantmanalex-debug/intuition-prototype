# Documentation only: the app reads environment variables, not this file.
INTUITION_MODE=demo
INTUITION_DB_PATH=.runtime/intuition.sqlite3
# No provider, endpoint, or credentials are required for demo mode.
# Remote mode is reserved and fails explicitly until an adapter is implemented.
